scripts/settings-plan.sh

# Unpacked #440 corpus evidence — tracked form is docs/data/corpus-440/files.tar.gz.
# Loose files are excluded because pre-commit formatters (shfmt, prettier) rewrite
# them, shifting the line numbers the measurement indexes.
docs/data/corpus-440/files/
